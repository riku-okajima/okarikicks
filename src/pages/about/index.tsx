import React from "react";
import Layout from "../../components/templates/Layout"

const about = () => {
    return (
        <Layout title="Okarikicks | About">

        </Layout>
    )
}

export default React.memo(about);